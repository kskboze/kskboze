# このファイルを「.env」という名前でコピーして使ってください。
# （.env は Git に登録されません。API キーが外に漏れないようにするためです）

# AIで案内文を作りたい場合だけ設定してください。空のままでも動きます。
# キーの取得先: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# 使用するAIモデル（通常は変更不要）
AI_MODEL=claude-sonnet-5

# データベースファイルの場所（通常は変更不要）
DATABASE_PATH=data/golf.db
